# -----------------------------------------------------------------------------
# Sample environment configuration for the Salesforce API
# Prefix: SALESFORCE_
# Nested config delimiter: underscore (_)
#
# This file reflects:
# - Config (global app settings)
# - ServerConfig (server and CORS settings)
# - DatabaseConfig (PostgreSQL settings)
#
# Copy this file to `.env` and adjust values for your environment.
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# Global application settings (Config)
# -----------------------------------------------------------------------------
SALESFORCE_APP_NAME=Salesforce
SALESFORCE_APP_VERSION=0.1.0

# Enables debug mode (True/False)
SALESFORCE_DEBUG=False

# Environment name (e.g., DEV, STAGE, PROD)
SALESFORCE_ENVIRONMENT=PROD


# -----------------------------------------------------------------------------
# Server settings (ServerConfig)
# -----------------------------------------------------------------------------

# Network configuration
SALESFORCE_SERVER__HOST=0.0.0.0
SALESFORCE_SERVER__PORT=80
SALESFORCE_SERVER__WORKERS=4
SALESFORCE_SERVER__RELOAD=False

# CORS configuration
SALESFORCE_SERVER__CORS_ENABLED=True
# Comma-separated list of allowed origins
SALESFORCE_SERVER__CORS_ORIGINS="*"
SALESFORCE_SERVER__CORS_ALLOW_CREDENTIALS=True
# Comma-separated list. Use * to allow all methods__
SALESFORCE_SERVER__CORS_ALLOW_METHODS=*
# Comma-separated list. Use * to allow all headers__
SALESFORCE_SERVER__CORS_ALLOW_HEADERS="*"

# Request limits
# Maximum request body size in bytes (default: 10 MB)
SALESFORCE_SERVER__MAX_REQUEST_SIZE=10485760
# Request timeout in seconds
SALESFORCE_SERVER__REQUEST_TIMEOUT=60

# Rate limiting
SALESFORCE_SERVER__RATE_LIMIT_ENABLED=False
SALESFORCE_SERVER__RATE_LIMIT_PER_MINUTE=60


# -----------------------------------------------------------------------------
# PostgreSQL database settings (DatabaseConfig)
# -----------------------------------------------------------------------------
SALESFORCE_POSTGRES__HOST=ep-crimson-fire-ahrbcwsz.c-3.us-east-1.aws.neon.tech
SALESFORCE_POSTGRES__PORT=5432
SALESFORCE_POSTGRES__NAME=neondb
SALESFORCE_POSTGRES__USER=neondb_owner
SALESFORCE_POSTGRES__PASSWORD=npg_dJvb47cUezum

# Connection pool configuration
SALESFORCE_POSTGRES__POOL_MIN_SIZE=1
SALESFORCE_POSTGRES__POOL_MAX_SIZE=4
SALESFORCE_POSTGRES__POOL_MAX_INACTIVE_CONNECTION_LIFETIME=300.0
SALESFORCE_POSTGRES__POOL_TIMEOUT=10.0

# -----------------------------------------------------------------------------
# Notes:
# - Boolean values should be True/False (case-insensitive due to case_sensitive=False).
# - Lists (origins, methods, headers) can be comma-separated strings.
# - Do not commit your real `.env` with secrets to version control.
# -----------------------------------------------------------------------------

SALESFORCE_SENTRY_DSN = https://a0a16236f88452610071a2e2a6735f4c@o4509650312167424.ingest.de.sentry.io/4510538964271184
